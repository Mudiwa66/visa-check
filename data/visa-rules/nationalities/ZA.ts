import { DestinationRules } from '../types';

const rules: DestinationRules = {
  TH: {
    type: 'visa-free',
    maxStay: '30 days',
    notes: 'Tourist purposes only. Must have proof of onward travel.',
    source: 'Thai Embassy, Jan 2026',
  },
};

export default rules;
